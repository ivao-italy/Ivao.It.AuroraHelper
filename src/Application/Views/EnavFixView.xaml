<UserControl
    x:Class="Ivao.It.AuroraHelper.Application.Views.EnavFixView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Ivao.It.AuroraHelper.Application.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="80" />
            <RowDefinition Height="65" />
        </Grid.RowDefinitions>

        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="25"
            FontWeight="Bold"
            Foreground="White">
            ENAV FIX EXPORT
        </TextBlock>
        <GroupBox Grid.Row="2" Margin="5">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top">END 4.4.1 File</TextBlock>
                <Button
                    x:Name="BrowseForFile"
                    Width="50"
                    Margin="2"
                    DockPanel.Dock="Right">
                    ...
                </Button>
                <TextBox
                    x:Name="FilePath"
                    Margin="2"
                    IsEnabled="False" />
            </DockPanel>
        </GroupBox>
        <TextBlock
            Grid.Row="1"
            d:Background="Azure"
            TextWrapping="Wrap">
            Identifica i fix in base al contenuto delle colonne 3, 4 e 5 della tabella del file<Bold>ENR 4.4.1</Bold>
            del ciclo AIP secondo la seguente logica:<LineBreak /><LineBreak />
            -<Bold>tasto ALL</Bold>
            : crea un file unico di tutti i FIX presenti in ENR 4.4.1 (colonne 3,4,5).<LineBreak />
            -<Bold>tasto FRA E/X</Bold>
            : crea un file di tutti i FIX categorizzati come E, X o EX nella colonna 5. questi fix sono quelli in ingresso ed uscita dallo spazio aereo FRAIT e servono per la creazione della mappa "FRA BDRY" attivabile con il testo "ACC" su Aurora</TextBlock>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style BasedOn="{StaticResource MahApps.Styles.Button}" TargetType="Button">
                    <Setter Property="Margin" Value="5" />
                </Style>
            </Grid.Resources>

            <Button x:Name="ExportAll">All</Button>
            <Button x:Name="ExportBoundary" Grid.Column="1">FRA E/X</Button>
            <!--<Button x:Name="ExportEnroute" Grid.Column="1">Enroute</Button>
            <Button x:Name="ExportTerminal" Grid.Column="2">Terminal</Button>-->

        </Grid>
    </Grid>
</UserControl>
